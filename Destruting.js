const object = { name: "laluprasath", age: 21, city: "karur" };

const { name, city } = object;
console.log(name);

// array destructing
const arr = ["walking", "sports", "video games"];
const [first, second, third] = arr;
console.log(first);
